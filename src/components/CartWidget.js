import React from 'react';

const CartWidget = () => {
  return (
    <div>
      <span>🛒</span> {/* Puedes poner un ícono más bonito aquí */}
      <span> 0 </span> {/* Número de artículos en el carrito */}
    </div>
  );
};

export default CartWidget;
